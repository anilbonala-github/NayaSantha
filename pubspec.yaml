name: naya_santha
description: NayaSantha - Your AI-Powered Weekly Market. Customer app for Android, iOS and Web.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  provider: ^6.1.2
  intl: ^0.19.0
  # Dynamic backend client (Vol2 §2): REST via Dio, Riverpod state, secure tokens.
  dio: ^5.7.0
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.2
  # Native Razorpay Checkout for Android/iOS (web uses checkout.js). Mobile-only plugin.
  razorpay_flutter: ^1.3.7
  # Firebase Cloud Messaging push (Vol1 FCM). firebase_core inits from firebase_options.dart.
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  remove_alpha_ios: true
